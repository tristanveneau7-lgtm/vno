# Named Cloudflare Tunnel URL for the engine. Points at your custom subdomain
# (e.g. https://engine.vnoweb.ca). Set once and forget — the tunnel UUID is
# fixed per machine, so this URL never changes between sessions.
VITE_ENGINE_URL=https://engine.your-domain.example
